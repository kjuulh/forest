.playwright-mcp/
*.png
overview-with-readme.png
.env
.env.*
!.env.example
.forest/

# Design references are the exception to the blanket *.png above: they are the
# material a design was worked from, they are linked from design/*.md, and a
# note pointing at four images nobody else has is not a note.
!design/references/**/*.png

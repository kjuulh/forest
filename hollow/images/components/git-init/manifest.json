{
    "name": "git-init",
    "version": "v1",
    "description": "Initialize a fresh git repo at $FOREST_WORK_DIR with an empty initial commit, configured author identity, and a chosen branch name.",
    "methods": [
        {
            "name": "git-init",
            "kind": "command",
            "description": "git init -b <branch>, configure user.name/user.email, create an empty commit. Outputs branch and initial_commit_sha."
        }
    ]
}

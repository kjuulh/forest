# Build output: pre-populated Forest component cache, regenerated by
# hollow-test-harness on every harness invocation. Tracking it would
# blow up git history with binary diffs every time a component changes.
forest-cache/

# Per-component artifacts staged into images/ for the Dockerfile to
# COPY in. Binary, regenerated on every build.
hollow-guest
forest
